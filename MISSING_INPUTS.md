# Missing or intentionally unavailable inputs

No input is missing for the headline reproduction when the three pinned archives in `data/manifest.json` are downloaded.

One requested artefact could not be recovered: there is no separate manual-adjudication table for approximately 60 observations. The source analysis instead contains 60 Chemspeed transfer-endpoint records produced by deterministic parsing and exact decimal comparison. They are regenerated as `data/derived/observations.csv` and labelled `automated_exact_decimal_comparison`.

Do not describe those 60 rows as an independently documented manual review unless a separate source file and classification protocol are provided later.

